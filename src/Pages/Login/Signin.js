import React from "react";
import SigninForm from "../../components/Login/SigninForm";

// Login form to existing form page
const Signin = () => {
     return (
          <span>
               <SigninForm />
          </span>
     );
};

export default Signin;
