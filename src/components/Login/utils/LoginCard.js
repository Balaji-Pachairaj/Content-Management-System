import React from "react";

//Simple carrying for both form - sign in and sign up
const LoginCard = (props) => {
     return <div className=" w-fit h-fit ">{props.children}</div>;
};

export default LoginCard;
